; MinZ generated code
; Generated: 2025-08-01 21:44:35


; Data section
    ORG $F000

.Users.alice.dev.minz-ts.examples.arrays.old_array:
    DS 10
.Users.alice.dev.minz-ts.examples.arrays.new_array:
    DS 10
.Users.alice.dev.minz-ts.examples.arrays.items:
    DS 15

; Code section
    ORG $8000

; Using hierarchical register allocation (physical → shadow → memory)

; Function: .Users.alice.dev.minz-ts.examples.arrays.main
.Users.alice.dev.minz-ts.examples.arrays.main:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
    PUSH DE
    ; r2 = addr(.Users.alice.dev.minz-ts.examples.arrays.old_array)
    LD HL, .Users.alice.dev.minz-ts.examples.arrays.old_array
    ; Register 2 already in HL
    ; Store to array[index] (u8)
    PUSH HL
    LD A, B
    POP HL
    LD (HL), A
    ; r6 = addr(.Users.alice.dev.minz-ts.examples.arrays.new_array)
    LD HL, .Users.alice.dev.minz-ts.examples.arrays.new_array
    LD D, H
    LD E, L
    ; Store to array[index] (u8)
    PUSH HL
    EXX               ; Switch to shadow registers
    LD A, B'         ; From shadow B'
    EXX               ; Switch back to main registers
    POP HL
    LD (HL), A
    ; r9 = 1
    LD A, 1
    EXX               ; Switch to shadow registers
    LD C', A         ; Store to shadow C'
    EXX               ; Switch back to main registers
    ; r10 = addr(.Users.alice.dev.minz-ts.examples.arrays.items)
    LD HL, .Users.alice.dev.minz-ts.examples.arrays.items
    LD B, H
    LD C, L
    ; Load array element (.Users.alice.dev.minz-ts.examples.arrays.Item)
    LD H, B
    LD L, C
    PUSH HL
    EXX               ; Switch to shadow registers
    POP HL
    ADD HL, DE
    LD A, (HL)
    EXX               ; Switch to shadow registers
    LD L', A         ; Store to shadow HL'
    EXX               ; Switch back to main registers
    ; Store to field id (offset 0)
    EXX               ; Switch to shadow registers
    PUSH HL           ; Save shadow HL
    EXX               ; Switch back to main registers
    POP HL            ; Load shadow HL into main HL
    PUSH HL
    POP DE
    LD (DE), L
    INC DE
    LD (DE), H
    ; return
    POP DE
    RET

; Runtime print helper functions
print_string:
    LD A, (HL)
    OR A               ; Check for null terminator
    RET Z              ; Return if null
    RST 16             ; Print character
    INC HL             ; Next character
    JR print_string

print_u8_decimal:
    LD H, 0            ; HL = A (zero extend)
    LD L, A
    CALL print_u16_decimal
    RET

print_u16_decimal:
    LD BC, -10000
    LD DE, -1000
    CALL print_digit
    LD BC, -1000
    LD DE, -100
    CALL print_digit
    LD BC, -100
    LD DE, -10
    CALL print_digit
    LD BC, -10
    LD DE, -1
    CALL print_digit
    LD A, L
    ADD A, '0'         ; Convert to ASCII
    RST 16             ; Print last digit
    RET

print_digit:
    LD A, '0'-1
print_digit_loop:
    INC A
    ADD HL, BC         ; Subtract power of 10
    JR C, print_digit_loop
    ADD HL, DE         ; Add back one power of 10
    RST 16             ; Print digit
    RET

print_i8_decimal:
    BIT 7, A           ; Check sign bit
    JR Z, print_u8_decimal
    PUSH AF
    LD A, '-'          ; Print minus sign
    RST 16
    POP AF
    NEG                ; Make positive
    JR print_u8_decimal

print_i16_decimal:
    BIT 7, H           ; Check sign bit
    JR Z, print_u16_decimal
    PUSH HL
    LD A, '-'          ; Print minus sign
    RST 16
    POP HL
    LD A, H            ; Negate HL
    CPL
    LD H, A
    LD A, L
    CPL
    LD L, A
    INC HL
    JR print_u16_decimal

print_bool:
    OR A               ; Test if A is zero
    JR NZ, print_true
    LD HL, bool_false_str
    JR print_string
print_true:
    LD HL, bool_true_str
    JR print_string

bool_true_str:
    DB "true", 0
bool_false_str:
    DB "false", 0


    END main
