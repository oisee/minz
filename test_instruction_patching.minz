// Test case for instruction patching TRUE SMC
// This should generate the revolutionary smart patching assembly

fun add_numbers(a: u8, b: u8) -> u8 {
    return a + b;
}

fun main() -> u8 {
    // Call 1: Immediate use - should generate early return patch
    let temp: u8 = add_numbers(10, 20) + 5;  // 30 + 5 = 35
    
    // Call 2: Storage - should generate store patch
    let stored_result: u8 = add_numbers(30, 40);  // 70
    
    // Final computation
    return temp + stored_result;  // 35 + 70 = 105
}