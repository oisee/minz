type TestBits = bits {
    field1: 3,
    field2: 5
};

fun main() -> void {
    let val: u8 = 0x42;
    let bits_val: TestBits = val as TestBits;
    
    // Only test field read access
    let f1: u8 = bits_val.field1;
}