# VIR Solver Showcase — Source → Z80 Assembly

Generated by Z3 joint isel+regalloc (provably optimal per basic block).
Every function assembles to valid Z80 binary via MZA.

## add ✓
```nanz
fun add(a: u8, b: u8) -> u8 { return a + b }
```
```asm
; add — VIR codegen (CFG-aware)
add:
    ADD A, C
    RET
```
**2 instructions, 2 bytes**

## sub ✓
```nanz
fun sub(a: u8, b: u8) -> u8 { return a - b }
```
```asm
; sub — VIR codegen (CFG-aware)
sub:
    SUB C
    RET
```
**2 instructions, 2 bytes**

## double ✓
```nanz
fun double(x: u8) -> u8 { return x + x }
```
```asm
; double — VIR codegen (CFG-aware)
double:
    ADD A, A
    RET
```
**2 instructions, 2 bytes**

## inc ✓
```nanz
fun inc(x: u8) -> u8 { return x + 1 }
```
```asm
; inc — VIR codegen (CFG-aware)
inc:
    LD B, 1
    INC A
    RET
```
**3 instructions, 4 bytes**

## identity ✓
```nanz
fun identity(x: u8) -> u8 { return x }
```
```asm
; identity — VIR codegen (CFG-aware)
identity:
    RET
```
**1 instructions, 1 bytes**

## const5 ✓
```nanz
fun const5() -> u8 { return 5 }
```
```asm
; const5 — VIR codegen (CFG-aware)
const5:
    LD C, 5
    LD A, C
    RET
```
**3 instructions, 4 bytes**

## negate ✓
```nanz
fun negate(x: u8) -> u8 { return 0 - x }
```
```asm
; negate — VIR codegen
negate:
    XOR A
    SUB D
    RET
```
**3 instructions, 3 bytes**

## band ✓
```nanz
fun band(a: u8, b: u8) -> u8 { return a & b }
```
```asm
; band — VIR codegen (CFG-aware)
band:
    AND C
    RET
```
**2 instructions, 2 bytes**

## bor ✓
```nanz
fun bor(a: u8, b: u8) -> u8 { return a | b }
```
```asm
; bor — VIR codegen (CFG-aware)
bor:
    OR C
    RET
```
**2 instructions, 2 bytes**

## ret_b ✓
```nanz
fun ret_b(a: u8, b: u8) -> u8 { return b }
```
```asm
; ret_b — VIR codegen (CFG-aware)
ret_b:
    LD A, C
    RET
```
**2 instructions, 2 bytes**

## swap ✓
```nanz
fun swap(a: u8, b: u8) -> u8 {
    let t: u8 = a
    return b
}
```
```asm
; swap — VIR codegen (CFG-aware)
swap:
    LD A, C
    RET
```
**2 instructions, 2 bytes**

## max ✓
```nanz
fun max(a: u8, b: u8) -> u8 {
    if a > b { return a }
    return b
}
```
```asm
; max — VIR codegen (CFG-aware)
max:
    CP C
    JR Z, .max_if_join2
    JR C, .max_if_join2
.max_if_then1:
    RET
.max_if_join2:
    LD A, C
    RET
```
**6 instructions, 8 bytes**

## min ✓
```nanz
fun min(a: u8, b: u8) -> u8 {
    if a < b { return a }
    return b
}
```
```asm
; min — VIR codegen (CFG-aware)
min:
    CP C
    JR NC, .min_if_join2
.min_if_then1:
    RET
.min_if_join2:
    LD A, C
    RET
```
**5 instructions, 6 bytes**

## abs_diff ✓
```nanz
fun abs_diff(a: u8, b: u8) -> u8 {
    if a > b { return a - b }
    return b - a
}
```
```asm
; abs_diff — VIR codegen
abs_diff:
    CP C
    JR Z, .abs_diff_if_join2
    JR C, .abs_diff_if_join2
.abs_diff_if_then1:
    LD L, A
    LD H, C
    SUB H
    RET
.abs_diff_if_join2:
    LD B, C
    LD L, A
    LD A, B
    SUB L
    RET
```
**12 instructions, 14 bytes**

## clamp_lo ✓
```nanz
fun clamp_lo(x: u8, lo: u8) -> u8 {
    if x < lo { return lo }
    return x
}
```
```asm
; clamp_lo — VIR codegen (CFG-aware)
clamp_lo:
    CP C
    JR NC, .clamp_lo_if_join2
.clamp_lo_if_then1:
    LD A, C
    RET
.clamp_lo_if_join2:
    RET
```
**5 instructions, 6 bytes**

## add_sub ✓
```nanz
fun add_sub(a: u8, b: u8, c: u8) -> u8 {
    return a + b - c
}
```
```asm
; add_sub — VIR codegen (CFG-aware)
add_sub:
    LD L, A
    ADD A, C
    SUB B
    RET
```
**4 instructions, 4 bytes**

## double_add ✓
```nanz
fun double_add(a: u8, b: u8) -> u8 {
    return a + a + b
}
```
```asm
; double_add — VIR codegen (CFG-aware)
double_add:
    LD D, A
    LD L, A
    ADD A, L
    ADD A, C
    RET
```
**5 instructions, 5 bytes**

## gcd ✓
```nanz
fun gcd(a: u8, b: u8) -> u8 {
    while a != b {
        if a > b { a = a - b }
        else     { b = b - a }
    }
    return a
}
```
```asm
; gcd — VIR codegen
gcd:
.gcd_loop_head1:
    CP B
    JR Z, .gcd_loop_exit3
.gcd_loop_body2:
    LD L, A
    LD L, B
    CP L
    JR Z, .gcd_if_else6
    JR C, .gcd_if_else6
.gcd_if_then4:
    LD C, A
    LD D, B
    LD A, C
    SUB D
.gcd_if_join5:
    JP .gcd_loop_head1
.gcd_if_else6:
    LD A, B
    ; trunc DE->E (alias)
    SUB E
    JP .gcd_if_join5
.gcd_loop_exit3:
    RET
```
**16 instructions, 23 bytes**

## fib ✓
```nanz
fun fib(n: u8) -> u16 {
    if n < 2 { return n }
    let a: u16 = 0
    let b: u16 = 1
    for i in 2..n+1 {
        let t: u16 = a + b
        a = b
        b = t
    }
    return b
}
```
```asm
; fib — VIR codegen (CFG-aware)
fib:
    LD H, 2
    CP 2
    JR NC, .fib_if_join2
.fib_if_then1:
    RET
.fib_if_join2:
    LD L, 0
    LD L, 1
    LD H, 2
    LD L, 1
    INC A
.fib_loop_head4:
    CP L
    JR NC, .fib_loop_exit6
.fib_loop_body5:
    EX DE, HL
    ADD HL, BC
    LD L, 1
    LD B, A
    INC B
    JP .fib_loop_head4
.fib_loop_exit6:
    RET
```
**18 instructions, 29 bytes**

## triple ✓
```nanz
fun triple(x: u8) -> u8 { return x + x + x }
```
```asm
; triple — VIR codegen (CFG-aware)
triple:
    LD L, A
    LD L, A
    ADD A, L
    ADD A, L
    RET
```
**5 instructions, 5 bytes**

## avg2 — SKIP (parse: line 2: unexpected token ">>" in expression)

## is_zero ✓
```nanz
fun is_zero(x: u8) -> u8 {
    if x == 0 { return 1 }
    return 0
}
```
```asm
; is_zero — VIR codegen (CFG-aware)
is_zero:
    LD H, 0
    OR A
    JR NZ, .is_zero_if_join2
.is_zero_if_then1:
    LD B, 1
    LD A, B
    RET
.is_zero_if_join2:
    LD L, 0
    LD A, L
    RET
```
**9 instructions, 13 bytes**

## max3 ✓
```nanz
fun max3(a: u8, b: u8, c: u8) -> u8 {
    if a > b {
        if a > c { return a }
        return c
    }
    if b > c { return b }
    return c
}
```
```asm
; max3 — VIR codegen
max3:
    CP C
    JR Z, .max3_if_join2
    JR C, .max3_if_join2
.max3_if_then1:
    LD E, A
    LD D, B
    LD A, E
    CP D
    JR Z, .max3_if_join4
    JR C, .max3_if_join4
.max3_if_then3:
    RET
.max3_if_join4:
    LD C, B
    LD B, C
    LD A, B
    RET
.max3_if_join2:
    LD A, C
    LD C, B
    CP C
    JR Z, .max3_if_join6
    JR C, .max3_if_join6
.max3_if_then5:
    LD B, C
    LD A, B
    RET
.max3_if_join6:
    LD C, B
    LD B, C
    LD A, B
    RET
```
**26 instructions, 32 bytes**

---
**Total: 22/22 showcase functions compiled + assembled**



/tmp/vir_random.go:12:2: no required module provides package github.com/minz/minzc/pkg/c89: go.mod file not found in current directory or any parent directory; see 'go help modules'
/tmp/vir_random.go:13:2: no required module provides package github.com/minz/minzc/pkg/hir: go.mod file not found in current directory or any parent directory; see 'go help modules'
/tmp/vir_random.go:14:2: no required module provides package github.com/minz/minzc/pkg/mir2: go.mod file not found in current directory or any parent directory; see 'go help modules'
/tmp/vir_random.go:15:2: no required module provides package github.com/minz/minzc/pkg/nanz: go.mod file not found in current directory or any parent directory; see 'go help modules'
/tmp/vir_random.go:16:2: no required module provides package github.com/minz/minzc/pkg/vir: go.mod file not found in current directory or any parent directory; see 'go help modules'
