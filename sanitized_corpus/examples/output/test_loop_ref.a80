; MinZ generated code
; Generated: 2025-08-01 09:30:35


; Data section
    ORG $F000

_examples_test_loop_ref_counters:
    DS 20

; Code section
    ORG $8000

; Using hierarchical register allocation (physical → shadow → memory)

; Function: ...examples.test_loop_ref.main
_examples_test_loop_ref_main:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
    ; r2 = 0
    LD A, 0
    ; Register 2 already in A
    ; store , r2
    ; Register 2 already in A
    LD ($F002), A
    ; loop_1:
loop_1:
    ; r3 = load i
    LD A, ($F002)
    LD C, A         ; Store to physical register C
    ; r4 = 10
    LD A, 10
    LD D, A         ; Store to physical register D
    ; r5 = r3 < r4
    LD D, H
    LD E, L
    EX DE, HL
    OR A      ; Clear carry
    SBC HL, DE
    JP M, .L1
    LD HL, 0
    JP .L2
.L1:
    LD HL, 1
.L2:
    ; jump_if_not r5, end_loop_2
    LD A, E
    OR A
    JP Z, end_loop_2
    ; r6 = 0
    LD A, 0
    LD H, A         ; Store to physical register H
    ; r7 = addr(...examples.test_loop_ref.counters)
    LD HL, ...examples.test_loop_ref.counters
    LD ($F00E), HL    ; Virtual register 7 to memory
    ; r8 = load i
    LD A, ($F002)
    EXX               ; Switch to shadow registers
    LD B', A         ; Store to shadow B'
    EXX               ; Switch back to main registers
    ; Load array element (...examples.test_loop_ref.Counter)
    LD HL, ($F00E)    ; Virtual register 7 from memory
    PUSH HL
    EXX               ; Switch to shadow registers
    POP HL
    ADD HL, DE
    LD A, (HL)
    LD E, A         ; Store to DE (low byte)
    ; Store to field value (offset 0)
    LD H, D
    LD L, E
    PUSH HL
    POP DE
    LD (DE), L
    INC DE
    LD (DE), H
    ; r10 = load i
    LD A, ($F002)
    EXX               ; Switch to shadow registers
    LD C', A         ; Store to shadow C'
    EXX               ; Switch back to main registers
    ; r11 = 5
    LD A, 5
    EXX               ; Switch to shadow registers
    LD D', A         ; Store to shadow D'
    EXX               ; Switch back to main registers
    ; r12 = r10 + r11
    LD D, H
    LD E, L
    ADD HL, DE
    ; r13 = addr(...examples.test_loop_ref.counters)
    LD HL, ...examples.test_loop_ref.counters
    LD B, H
    LD C, L
    ; r14 = load i
    LD A, ($F002)
    LD L, A         ; Store to HL (low byte)
    ; Load array element (...examples.test_loop_ref.Counter)
    LD H, B
    LD L, C
    PUSH HL
    LD D, H
    LD E, L
    POP HL
    ADD HL, DE
    LD A, (HL)
    LD ($F01E), A     ; Virtual register 15 to memory
    ; Store to field max (offset 1)
    LD HL, ($F01E)    ; Virtual register 15 from memory
    LD DE, 1
    ADD HL, DE
    PUSH HL
    POP DE
    LD (DE), L
    INC DE
    LD (DE), H
    ; r16 = load i
    LD A, ($F002)
    LD ($F020), A     ; Virtual register 16 to memory
    ; r17 = 1
    LD A, 1
    LD H, A         ; Store to physical register H
    ; r18 = r16 + r17
    LD HL, ($F020)    ; Virtual register 16 from memory
    LD D, H
    LD E, L
    ADD HL, DE
    ; store i, r18
    LD A, L
    LD ($F002), A
    ; jump loop_1
    JP loop_1
    ; end_loop_2:
end_loop_2:
    ; r19 = 1
    LD A, 1
    LD L, A         ; Store to HL (low byte)
    ; return
    RET

; Runtime print helper functions
print_string:
    LD A, (HL)
    OR A               ; Check for null terminator
    RET Z              ; Return if null
    RST 16             ; Print character
    INC HL             ; Next character
    JR print_string

print_u8_decimal:
    LD H, 0            ; HL = A (zero extend)
    LD L, A
    CALL print_u16_decimal
    RET

print_u16_decimal:
    LD BC, -10000
    LD DE, -1000
    CALL print_digit
    LD BC, -1000
    LD DE, -100
    CALL print_digit
    LD BC, -100
    LD DE, -10
    CALL print_digit
    LD BC, -10
    LD DE, -1
    CALL print_digit
    LD A, L
    ADD A, '0'         ; Convert to ASCII
    RST 16             ; Print last digit
    RET

print_digit:
    LD A, '0'-1
print_digit_loop:
    INC A
    ADD HL, BC         ; Subtract power of 10
    JR C, print_digit_loop
    ADD HL, DE         ; Add back one power of 10
    RST 16             ; Print digit
    RET

print_i8_decimal:
    BIT 7, A           ; Check sign bit
    JR Z, print_u8_decimal
    PUSH AF
    LD A, '-'          ; Print minus sign
    RST 16
    POP AF
    NEG                ; Make positive
    JR print_u8_decimal

print_i16_decimal:
    BIT 7, H           ; Check sign bit
    JR Z, print_u16_decimal
    PUSH HL
    LD A, '-'          ; Print minus sign
    RST 16
    POP HL
    LD A, H            ; Negate HL
    CPL
    LD H, A
    LD A, L
    CPL
    LD L, A
    INC HL
    JR print_u16_decimal

print_bool:
    OR A               ; Test if A is zero
    JR NZ, print_true
    LD HL, bool_false_str
    JR print_string
print_true:
    LD HL, bool_true_str
    JR print_string

bool_true_str:
    DB "true", 0
bool_false_str:
    DB "false", 0


    END main
