; MinZ generated code
; Generated: 2025-08-03 15:05:44


; Code section
    ORG $8000

; Using hierarchical register allocation (physical → shadow → memory)

; Function: ......examples.test_complex_assign_simple.test_arrays
_examples_test_complex_assign_simple_test_arrays:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
    ; Store to array[index] (u8)
    LD HL, ($F00A)    ; Virtual register 5 from memory
    PUSH HL
    LD A, ($F004)     ; Virtual register 2 from memory
    POP HL
    LD (HL), A
    ; Store to array[index] (u8)
    PUSH HL
    LD A, D
    POP HL
    LD (HL), A
    ; Store to array[index] (u8)
    PUSH HL
    LD A, H
    POP HL
    LD (HL), A
    ; Store to array[index] (u8)
    PUSH HL
    EXX               ; Switch to shadow registers
    LD A, B'         ; From shadow B'
    EXX               ; Switch back to main registers
    POP HL
    LD (HL), A
    ; Load array element (u8)
    LD HL, ($F028)    ; Virtual register 20 from memory
    PUSH HL
    EXX               ; Switch to shadow registers
    LD A, E'         ; From shadow E'
    EXX               ; Switch back to main registers
    LD E, A
    LD D, 0
    POP HL
    ADD HL, DE
    LD A, (HL)
    LD ($F02C), A     ; Virtual register 22 to memory
    ; Load array element (u8)
    LD HL, ($F02E)    ; Virtual register 23 from memory
    PUSH HL
    LD A, B
    LD E, A
    LD D, 0
    POP HL
    ADD HL, DE
    LD A, (HL)
    LD B, A         ; Store to physical register B
    ; r26 = r22 + r25
    LD HL, ($F02C)    ; Virtual register 22 from memory
    LD D, H
    LD E, L
    ADD HL, DE
    ; Load array element (u8)
    PUSH HL
    LD A, B
    LD E, A
    LD D, 0
    POP HL
    ADD HL, DE
    LD A, (HL)
    EXX               ; Switch to shadow registers
    LD D', A         ; Store to shadow D'
    EXX               ; Switch back to main registers
    ; r30 = r26 + r29
    LD D, H
    LD E, L
    ADD HL, DE
    ; Load array element (u8)
    PUSH HL
    LD A, B
    LD E, A
    LD D, 0
    POP HL
    ADD HL, DE
    LD A, (HL)
    EXX               ; Switch to shadow registers
    LD C', A         ; Store to shadow C'
    EXX               ; Switch back to main registers
    ; r34 = r30 + r33
    LD D, H
    LD E, L
    ADD HL, DE
    ; return r34
    RET
; Using hierarchical register allocation (physical → shadow → memory)

; Function: ......examples.test_complex_assign_simple.test_structs
_examples_test_complex_assign_simple_test_structs:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
    PUSH DE
    ; r6 = load v
    LD HL, ($F002)
    LD ($F00C), HL    ; Virtual register 6 to memory
    ; Load field x (offset 0)
    LD HL, ($F00C)    ; Virtual register 6 from memory
    LD E, (HL)
    INC HL
    LD D, (HL)
    EX DE, HL
    ; r8 = load v
    LD HL, ($F002)
    ; Load field y (offset 1)
    LD DE, 1
    ADD HL, DE
    LD E, (HL)
    INC HL
    LD D, (HL)
    EX DE, HL
    ; r10 = r7 + r9
    LD D, H
    LD E, L
    ADD HL, DE
    ; return r10
    POP DE
    RET
; Using hierarchical register allocation (physical → shadow → memory)

; Function: ......examples.test_complex_assign_simple.main
_examples_test_complex_assign_simple_main:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
    PUSH BC
    PUSH DE
    ; r2 = call test_arrays
    ; Call to test_arrays (args: 0)
    ; Found function, UsesTrueSMC=false
    CALL ......examples.test_complex_assign_simple.test_arrays
    ; r4 = call test_structs
    ; Call to test_structs (args: 0)
    ; Found function, UsesTrueSMC=false
    CALL ......examples.test_complex_assign_simple.test_structs
    ; r5 = load a
    LD A, ($F002)
    ; Register 5 already in A
    ; return r5
    POP DE
    POP BC
    RET

; Runtime print helper functions
print_string:
    LD B, (HL)         ; B = length from first byte
    INC HL             ; HL -> string data
    LD A, B            ; Check if length is zero
    OR A
    RET Z              ; Return if empty string
print_loop:
    LD A, (HL)         ; Load character
    RST 16             ; Print character
    INC HL             ; Next character
    DJNZ print_loop    ; Decrement B and loop
    RET

print_u8_decimal:
    LD H, 0            ; HL = A (zero extend)
    LD L, A
    CALL print_u16_decimal
    RET

print_u16_decimal:
    LD BC, -10000
    LD DE, -1000
    CALL print_digit
    LD BC, -1000
    LD DE, -100
    CALL print_digit
    LD BC, -100
    LD DE, -10
    CALL print_digit
    LD BC, -10
    LD DE, -1
    CALL print_digit
    LD A, L
    ADD A, '0'         ; Convert to ASCII
    RST 16             ; Print last digit
    RET

print_digit:
    LD A, '0'-1
print_digit_loop:
    INC A
    ADD HL, BC         ; Subtract power of 10
    JR C, print_digit_loop
    ADD HL, DE         ; Add back one power of 10
    RST 16             ; Print digit
    RET

print_i8_decimal:
    BIT 7, A           ; Check sign bit
    JR Z, print_u8_decimal
    PUSH AF
    LD A, '-'          ; Print minus sign
    RST 16
    POP AF
    NEG                ; Make positive
    JR print_u8_decimal

print_i16_decimal:
    BIT 7, H           ; Check sign bit
    JR Z, print_u16_decimal
    PUSH HL
    LD A, '-'          ; Print minus sign
    RST 16
    POP HL
    LD A, H            ; Negate HL
    CPL
    LD H, A
    LD A, L
    CPL
    LD L, A
    INC HL
    JR print_u16_decimal

print_bool:
    OR A               ; Test if A is zero
    JR NZ, print_true
    LD HL, bool_false_str
    JR print_string
print_true:
    LD HL, bool_true_str
    JR print_string

bool_true_str:
    DB 4, "true"      ; Length + data
bool_false_str:
    DB 5, "false"     ; Length + data


    END main
