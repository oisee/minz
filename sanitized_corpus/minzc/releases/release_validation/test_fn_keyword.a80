; MinZ generated code
; Generated: 2025-08-03 15:05:44


; Code section
    ORG $8000

; Using hierarchical register allocation (physical → shadow → memory)

; Function: ......examples.test_fn_keyword.add
_examples_test_fn_keyword_add:
; TRUE SMC function with immediate anchors
a_immOP:
    LD A, 0        ; a anchor (will be patched)
a_imm0 EQU a_immOP+1
b_immOP:
    LD B, 0        ; b anchor (will be patched)
b_imm0 EQU b_immOP+1
    ; r5 = r3 + r4
    LD D, H
    LD E, L
    ADD HL, DE
    ; return r5
    RET
; Using hierarchical register allocation (physical → shadow → memory)

; Function: ......examples.test_fn_keyword.subtract
_examples_test_fn_keyword_subtract:
; TRUE SMC function with immediate anchors
a_immOP:
    LD E, 0        ; a anchor (will be patched)
a_imm0 EQU a_immOP+1
b_immOP:
    LD A, 0        ; b anchor (will be patched)
b_imm0 EQU b_immOP+1
    LD H, A         ; Store to physical register H
    ; r5 = r3 - r4
    LD D, H
    LD E, L
    EX DE, HL
    OR A      ; Clear carry
    SBC HL, DE
    ; return r5
    RET
; Using hierarchical register allocation (physical → shadow → memory)

; Function: ......examples.test_fn_keyword.main
_examples_test_fn_keyword_main:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
    PUSH BC
    PUSH DE
    ; r4 = call add
    ; Call to add (args: 2)
    ; Stack-based parameter passing
    LD HL, ($F006)    ; Virtual register 3 from memory
    PUSH HL       ; Argument 1
    LD HL, ($F004)    ; Virtual register 2 from memory
    PUSH HL       ; Argument 0
    ; Found function, UsesTrueSMC=true
    ; TRUE SMC call to ......examples.test_fn_keyword.add
    LD A, ($F004)     ; Virtual register 2 from memory
    LD (a_imm0), A        ; Patch a
    LD A, ($F006)     ; Virtual register 3 from memory
    LD (b_imm0), A        ; Patch b
    CALL ......examples.test_fn_keyword.add
    ; r8 = call subtract
    ; Call to subtract (args: 2)
    ; Stack-based parameter passing
    LD HL, ($F00E)    ; Virtual register 7 from memory
    PUSH HL       ; Argument 1
    LD HL, ($F00C)    ; Virtual register 6 from memory
    PUSH HL       ; Argument 0
    ; Found function, UsesTrueSMC=true
    ; TRUE SMC call to ......examples.test_fn_keyword.subtract
    LD A, ($F00C)     ; Virtual register 6 from memory
    LD (a_imm0), A        ; Patch a
    LD A, ($F00E)     ; Virtual register 7 from memory
    LD (b_imm0), A        ; Patch b
    CALL ......examples.test_fn_keyword.subtract
    ; Direct print "Sum: " (5 chars)
    ; Direct print "Sum: " (5 chars)
    LD A, 83
    RST 16             ; Print character
    LD A, 117
    RST 16             ; Print character
    LD A, 109
    RST 16             ; Print character
    LD A, 58
    RST 16             ; Print character
    LD A, 32
    RST 16             ; Print character
    ; Direct print ", Diff: " (8 chars)
    ; Direct print ", Diff: " (8 chars)
    LD A, 44
    RST 16             ; Print character
    LD A, 32
    RST 16             ; Print character
    LD A, 68
    RST 16             ; Print character
    LD A, 105
    RST 16             ; Print character
    LD A, 102
    RST 16             ; Print character
    LD A, 102
    RST 16             ; Print character
    LD A, 58
    RST 16             ; Print character
    LD A, 32
    RST 16             ; Print character
    ; Direct print "
" (1 chars)
    ; Direct print "
" (1 chars)
    LD A, 10
    RST 16             ; Print character
    ; return
    POP DE
    POP BC
    RET

; TRUE SMC PATCH-TABLE
; Format: DW anchor_addr, DB size, DB param_tag
PATCH_TABLE:
    DW a_imm0           ; ......examples.test_fn_keyword.add.a
    DB 1              ; Size in bytes
    DB 0              ; Reserved for param tag
    DW b_imm0           ; ......examples.test_fn_keyword.add.b
    DB 1              ; Size in bytes
    DB 0              ; Reserved for param tag
    DW a_imm0           ; ......examples.test_fn_keyword.subtract.a
    DB 1              ; Size in bytes
    DB 0              ; Reserved for param tag
    DW b_imm0           ; ......examples.test_fn_keyword.subtract.b
    DB 1              ; Size in bytes
    DB 0              ; Reserved for param tag
    DW 0              ; End of table
PATCH_TABLE_END:

; Runtime print helper functions
print_string:
    LD B, (HL)         ; B = length from first byte
    INC HL             ; HL -> string data
    LD A, B            ; Check if length is zero
    OR A
    RET Z              ; Return if empty string
print_loop:
    LD A, (HL)         ; Load character
    RST 16             ; Print character
    INC HL             ; Next character
    DJNZ print_loop    ; Decrement B and loop
    RET

print_u8_decimal:
    LD H, 0            ; HL = A (zero extend)
    LD L, A
    CALL print_u16_decimal
    RET

print_u16_decimal:
    LD BC, -10000
    LD DE, -1000
    CALL print_digit
    LD BC, -1000
    LD DE, -100
    CALL print_digit
    LD BC, -100
    LD DE, -10
    CALL print_digit
    LD BC, -10
    LD DE, -1
    CALL print_digit
    LD A, L
    ADD A, '0'         ; Convert to ASCII
    RST 16             ; Print last digit
    RET

print_digit:
    LD A, '0'-1
print_digit_loop:
    INC A
    ADD HL, BC         ; Subtract power of 10
    JR C, print_digit_loop
    ADD HL, DE         ; Add back one power of 10
    RST 16             ; Print digit
    RET

print_i8_decimal:
    BIT 7, A           ; Check sign bit
    JR Z, print_u8_decimal
    PUSH AF
    LD A, '-'          ; Print minus sign
    RST 16
    POP AF
    NEG                ; Make positive
    JR print_u8_decimal

print_i16_decimal:
    BIT 7, H           ; Check sign bit
    JR Z, print_u16_decimal
    PUSH HL
    LD A, '-'          ; Print minus sign
    RST 16
    POP HL
    LD A, H            ; Negate HL
    CPL
    LD H, A
    LD A, L
    CPL
    LD L, A
    INC HL
    JR print_u16_decimal

print_bool:
    OR A               ; Test if A is zero
    JR NZ, print_true
    LD HL, bool_false_str
    JR print_string
print_true:
    LD HL, bool_true_str
    JR print_string

bool_true_str:
    DB 4, "true"      ; Length + data
bool_false_str:
    DB 5, "false"     ; Length + data


    END main
