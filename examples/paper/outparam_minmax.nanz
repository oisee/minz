// Nanz: native multi-return for minmax
fun minmax(a: u8, b: u8) -> (u8, u8) {
    if a <= b { return (a, b) }
    return (b, a)
}

fun get_min(a: u8, b: u8) -> u8 {
    let (mn, mx) = minmax(a, b)
    return mn
}

fun main() -> u8 { return get_min(10, 3) }
