// Test Lua math operations and functions

// Mathematical constants and functions
const PI_SCALED: u8 = @lua(math.floor(math.pi * 40));          // ~125
const E_SCALED: u8 = @lua(math.floor(math.exp(1) * 40));       // ~108

// Trigonometric calculations (scaled to fit in u8)
const COS_45_DEG: u8 = @lua(math.floor(math.cos(math.pi/4) * 180 + 50));  // ~177
const TAN_30_DEG: u8 = @lua(math.floor(math.tan(math.pi/6) * 100));       // ~57

// Power and logarithm
const POW_2_5: u8 = @lua(math.pow(2, 5));                      // 32
const LOG_10: u8 = @lua(math.floor(math.log(10) * 40));        // ~92

// Random number generation (seeded for consistency)
@lua[[[
math.randomseed(42)
function rand_byte()
    return math.random(0, 255)
end
]]]

const RANDOM_VALUE: u8 = @lua(rand_byte());

fun main() -> void {
    let pi = PI_SCALED;
    let e = E_SCALED;
    let cos45 = COS_45_DEG;
    let pow = POW_2_5;
    let rnd = RANDOM_VALUE;
    
    return;
}