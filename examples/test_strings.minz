// Test string literals

fun test_strings() -> void {
    let msg: *u8 = "Hello, World!";
    let ch: u8 = msg[0];  // Should be 'H'
    return;
}

fun main() -> void {
    test_strings();
    return;
}