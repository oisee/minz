// Generated by MinZ mir2gpu — Vulkan GLSL
#version 450
layout(local_size_x = 256) in;
layout(std430, binding = 0) buffer ResultBuf { uint results[]; };
layout(push_constant) uniform Params { uint n; };

void main() {
    uint tid = gl_GlobalInvocationID.x;
    if (tid >= n) return;
    uint r1 = tid;
    uint r2 = 0;

    r2 = (r1 + r1) & 0xFF;
    results[tid] = r2;
    return;
}

