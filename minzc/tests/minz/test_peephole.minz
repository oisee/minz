// Test peephole optimization patterns
fun test() -> void {
    let a: u8 = 5;
    let b: u8 = 10;
    
    // This should generate the LD D,H; LD E,L; EX DE,HL pattern
    if a < b {
        @print("a is less than b");
    }
}

fun main() -> void {
    test();
}