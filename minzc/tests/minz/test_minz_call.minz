// Test calling @minz generated functions

// Generate a simple function
@minz[[[fun greet_{0}() -> void { @print("Hello from {0}!"); }]]]("MinZ")

// Generate an add function
@minz[[[fun add_{0}(a: {0}, b: {0}) -> {0} { return a + b; }]]]("u8")

fun main() -> u8 {
    @print("Testing @minz generated function calls")
    
    // Call the generated greet function
    greet_MinZ();
    
    // Call the generated add function
    let result = add_u8(10, 20);
    @print("10 + 20 = { result }");
    
    return 0;
}