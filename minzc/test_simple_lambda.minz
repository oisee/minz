global numbers: [u8; 3] = [1, 2, 3];

fun double_it(x: u8) -> u8 {
    return x * 2;
}

fun main() -> u8 {
    // Test simple function call first
    numbers.iter().map(double_it).forEach(print_u8);
    
    return 42;
}