; MinZ generated code
; Generated: 2025-07-26 12:32:33


; Code section
    ORG $8000


; Function: ...examples.test_direct_return.calculate_once
...examples.test_direct_return.calculate_once:
; TRUE SMC function with immediate anchors
a$imm0:
    LD A, 0        ; a anchor (will be patched)
    LD ($F000), A
b$imm0:
    LD A, 0        ; b anchor (will be patched)
    LD ($F002), A
    LD A, (a$imm0)    ; Reuse from anchor
    LD ($F006), A
    ; return r3
    LD HL, ($F006)
    LD (result), HL    ; Direct return optimization
    RET
    RET

; Function: ...examples.test_direct_return.calculate_once2
...examples.test_direct_return.calculate_once2:
; TRUE SMC function with immediate anchors
a$imm0:
    LD A, 0        ; a anchor (will be patched)
    LD ($F000), A
b$imm0:
    LD A, 0        ; b anchor (will be patched)
    LD ($F002), A
    LD A, (a$imm0)    ; Reuse from anchor
    LD ($F006), A
    ; return r3
    LD HL, ($F006)
    RET
    RET

; Function: ...examples.test_direct_return.main
...examples.test_direct_return.main:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
    PUSH BC
    PUSH DE
    ; Optimized: return value directly stored to result
    CALL calculate_once
    LD ($F006), HL
    ; r7 = call calculate_once2
    CALL calculate_once2
    LD ($F00E), HL
    ; store , r7
    LD HL, ($F00E)
    LD ($F008), HL
    ; r10 = call calculate_once2
    CALL calculate_once2
    LD ($F014), HL
    ; store result, r10
    LD HL, ($F014)
    LD ($F000), HL
    ; return
    POP DE
    POP BC
    RET

    END main
