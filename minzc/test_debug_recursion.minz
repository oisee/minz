// Simple recursion test to debug the issue

fun simple_factorial(n: u8) -> u16 {
    if n <= 1 {
        return 1;
    }
    return n * simple_factorial(n - 1);
}

fun main() -> void {
    let result = simple_factorial(5);
}