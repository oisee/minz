; MinZ generated code
; Generated: 2025-08-11 09:33:46


; Data section
    ORG $F000

str_0:
    DB 32    ; Length
    DB "CTIE test compiled successfully!"
str_1:
    DB 41    ; Length
    DB "Results computed (maybe at compile-time!)"

; Code section
    ORG $8000

; Using hierarchical register allocation (physical → shadow → memory)

; Function: ...test_ctie_simple.add_const$u8$u8
...test_ctie_simple.add_const$u8$u8:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
...test_ctie_simple.add_const$u8$u8_param_a.op:
...test_ctie_simple.add_const$u8$u8_param_a equ ...test_ctie_simple.add_const$u8$u8_param_a.op + 1
    LD A, #00      ; Parameter a (gets patched)
    ; Register 3 already in A
...test_ctie_simple.add_const$u8$u8_param_b.op:
...test_ctie_simple.add_const$u8$u8_param_b equ ...test_ctie_simple.add_const$u8$u8_param_b.op + 1
    LD B, #00      ; Parameter b (gets patched)
    LD A, B
    LD B, A         ; Store to physical register B
    ; r5 = r3 + r4
    LD D, H
    LD E, L
    ADD HL, DE
    LD A, D

    ; *** SMART PATCHABLE RETURN SEQUENCE ***
    ; Default: Store to memory (most common complex case)
    ; For immediate use: Patch first NOP to RET for early return
...test_ctie_simple.add_const$u8$u8_return_patch.op:
    NOP                     ; PATCH POINT: NOP or RET (C9) for early return
...test_ctie_simple.add_const$u8$u8_store_addr.op:
...test_ctie_simple.add_const$u8$u8_store_addr equ ...test_ctie_simple.add_const$u8$u8_store_addr.op + 1
    LD (0000), A            ; DEFAULT: Store result (address gets patched)
    RET                     ; Return after store
; Using hierarchical register allocation (physical → shadow → memory)

; Function: ...test_ctie_simple.multiply_const$u8
...test_ctie_simple.multiply_const$u8:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
...test_ctie_simple.multiply_const$u8_param_x.op:
...test_ctie_simple.multiply_const$u8_param_x equ ...test_ctie_simple.multiply_const$u8_param_x.op + 1
    LD A, #00      ; Parameter x (gets patched)
    LD E, A         ; Store to physical register E
    ; r3 = 10
    LD A, 10
    LD H, A         ; Store to physical register H
    ; r4 = r2 * r3
    LD A, E
    ; Optimized x * 10 = (x << 3) + (x << 1)
    ADD A, A          ; x << 1
    LD B, A           ; Save (x << 1)
    ADD A, A          ; x << 2
    ADD A, A          ; x << 3
    ADD A, B          ; + (x << 1)
    LD L, A
    LD H, 0
    LD A, L

    ; *** SMART PATCHABLE RETURN SEQUENCE ***
    ; Default: Store to memory (most common complex case)
    ; For immediate use: Patch first NOP to RET for early return
...test_ctie_simple.multiply_const$u8_return_patch.op:
    NOP                     ; PATCH POINT: NOP or RET (C9) for early return
...test_ctie_simple.multiply_const$u8_store_addr.op:
...test_ctie_simple.multiply_const$u8_store_addr equ ...test_ctie_simple.multiply_const$u8_store_addr.op + 1
    LD (0000), A            ; DEFAULT: Store result (address gets patched)
    RET                     ; Return after store
; Using hierarchical register allocation (physical → shadow → memory)

; Function: ...test_ctie_simple.main
...test_ctie_simple.main:
; IsSMCDefault=true, IsSMCEnabled=true
; Using absolute addressing for locals (SMC style)
    ; r2 = 5
    LD A, 5
    EXX               ; Switch to shadow registers
    LD B', A         ; Store to shadow B'
    EXX               ; Switch back to main registers
    ; r3 = 3
    LD A, 3
    EXX               ; Switch to shadow registers
    LD C', A         ; Store to shadow C'
    EXX               ; Switch back to main registers
    ; r4 = 5
    LD A, 5
    LD ($F008), A     ; Virtual register 4 to memory
    ; r5 = 3
    LD A, 3
    EXX               ; Switch to shadow registers
    LD E', A         ; Store to shadow E'
    EXX               ; Switch back to main registers
    ; unknown op 39
    ; Smart patch 'store_u8' for ...test_ctie_simple.add_const$u8$u8_return_patch
    LD A, #00               ; NOP opcode
    LD (...test_ctie_simple.add_const$u8$u8_return_patch.op), A
    ; unknown op 40
    ; Patch storage address: temp_result
    LD HL, temp_result
    LD (...test_ctie_simple.add_const$u8$u8_store_addr), HL
    ; unknown op 41
    ; Patch parameter a = 0
    LD A, 0               ; Parameter value
    LD (...test_ctie_simple.add_const$u8$u8_param_a+1), A   ; Patch parameter immediate
    ; unknown op 41
    ; Patch parameter b = 0
    LD A, 0               ; Parameter value
    LD (...test_ctie_simple.add_const$u8$u8_param_b+1), A   ; Patch parameter immediate
    ; r6 = call ...test_ctie_simple.add_const$u8$u8
    ; Call to ...test_ctie_simple.add_const$u8$u8 (args: 2)
    ; Found function, UsesTrueSMC=false
    CALL ...test_ctie_simple.add_const$u8$u8
    ; store result1, r6
    LD ($F002), HL
    ; r8 = 4
    LD A, 4
    EXX               ; Switch to shadow registers
    LD D', A         ; Store to shadow D'
    EXX               ; Switch back to main registers
    ; r9 = 4
    LD A, 4
    LD L, A         ; Store to HL (low byte)
    ; unknown op 39
    ; Smart patch 'store_u8' for ...test_ctie_simple.multiply_const$u8_return_patch
    LD A, #00               ; NOP opcode
    LD (...test_ctie_simple.multiply_const$u8_return_patch.op), A
    ; unknown op 40
    ; Patch storage address: temp_result
    LD HL, temp_result
    LD (...test_ctie_simple.multiply_const$u8_store_addr), HL
    ; unknown op 41
    ; Patch parameter x = 0
    LD A, 0               ; Parameter value
    LD (...test_ctie_simple.multiply_const$u8_param_x+1), A   ; Patch parameter immediate
    ; r10 = call ...test_ctie_simple.multiply_const$u8
    ; Call to ...test_ctie_simple.multiply_const$u8 (args: 1)
    ; Found function, UsesTrueSMC=false
    CALL ...test_ctie_simple.multiply_const$u8
    ; store result2, r10
    LD ($F00E), HL
    ; r11 = string(str_0)
    LD HL, str_0
    ; Register 11 already in HL
    ; Print "CTIE test compiled successfully!" (32 chars via loop)
    ; Register 11 already in HL
    CALL print_string
    ; r12 = string(str_1)
    LD HL, str_1
    ; Print "Results computed (maybe at compile-time!)" (41 chars via loop)
    CALL print_string
    ; return
    RET

; Runtime print helper functions
print_string:
    LD A, (HL)         ; A = first byte
    CP 255             ; Check if extended format marker
    JR Z, print_string_u16
    ; Standard u8 format: [len:u8][data...]
    LD B, A            ; B = length from first byte
    INC HL             ; HL -> string data
    OR A               ; Check if length is zero
    RET Z              ; Return if empty string
print_loop_u8:
    LD A, (HL)         ; Load character
    RST 16             ; ZX Spectrum ROM print
    INC HL             ; Next character
    DJNZ print_loop_u8 ; Decrement B and loop
    RET

print_string_u16:
    ; Extended u16 format: [255][len:u16][data...]
    INC HL             ; Skip 255 marker
    LD E, (HL)         ; E = low byte of length
    INC HL
    LD D, (HL)         ; D = high byte of length
    INC HL             ; HL -> string data
    LD A, D            ; Check if length is zero
    OR E
    RET Z              ; Return if empty string
    ; Use 16-bit counter for large strings
print_loop_u16:
    LD A, (HL)         ; Load character
    RST 16             ; Print character
    INC HL             ; Next character
    DEC DE             ; Decrement 16-bit counter
    LD A, D            ; Check if counter is zero
    OR E
    JR NZ, print_loop_u16
    RET

print_u8_decimal:
    LD H, 0            ; HL = A (zero extend)
    LD L, A
    CALL print_u16_decimal
    RET

print_u16_decimal:
    LD BC, -10000
    LD DE, -1000
    CALL print_digit
    LD BC, -1000
    LD DE, -100
    CALL print_digit
    LD BC, -100
    LD DE, -10
    CALL print_digit
    LD BC, -10
    LD DE, -1
    CALL print_digit
    LD A, L
    ADD A, '0'         ; Convert to ASCII
    RST 16             ; Print last digit
    RET

print_digit:
    LD A, '0'-1
print_digit_loop:
    INC A
    ADD HL, BC         ; Subtract power of 10
    JR C, print_digit_loop
    ADD HL, DE         ; Add back one power of 10
    RST 16             ; Print digit
    RET

print_i8_decimal:
    BIT 7, A           ; Check sign bit
    JR Z, print_u8_decimal
    PUSH AF
    LD A, '-'          ; Print minus sign
    RST 16
    POP AF
    NEG                ; Make positive
    JR print_u8_decimal

print_i16_decimal:
    BIT 7, H           ; Check sign bit
    JR Z, print_u16_decimal
    PUSH HL
    LD A, '-'          ; Print minus sign
    RST 16
    POP HL
    LD A, H            ; Negate HL
    CPL
    LD H, A
    LD A, L
    CPL
    LD L, A
    INC HL
    JR print_u16_decimal

print_bool:
    OR A               ; Test if A is zero
    JR NZ, print_true
    LD HL, bool_false_str
    JR print_string
print_true:
    LD HL, bool_true_str
    JR print_string

bool_true_str:
    DB 4, "true"      ; Length + data
bool_false_str:
    DB 5, "false"     ; Length + data


    END main
