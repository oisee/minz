// Test simple struct declaration and usage
// TSMC-friendly: structs are just collections of static vars

struct Point {
    x: u8,
    y: u8
}

fun test_struct() -> void {
    let p: Point
    // In TSMC, p.x and p.y are just two static memory locations
}

fun main() -> void {
    test_struct()
}