// Simplest possible test for pattern guards

fun test_guard() -> u8 {
    var x: u8 = 5;
    case x {
        n if n > 0 => 1,
        _ => 0,
    }
}

pub fun main() -> u8 {
    return test_guard();
}