// Simple test of Lua blocks

// Define a Lua function
@lua[[[
function double(x)
    return x * 2
end
]]]

// Use it in a constant
const DOUBLED_5: u8 = @lua(double(5));

fun main() -> void {
    let x = DOUBLED_5;  // 10
    return;
}