// Simple test of Lua functions
@lua[[[
    print("Testing minz module")
    print("Type of minz:", type(minz))
    if minz then
        for k,v in pairs(minz) do
            print("  " .. k .. " = " .. type(v))
        end
    end
]]]

fun main() -> void {
    // Empty main
}