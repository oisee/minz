// Simple tail recursion test
fun simple_tail(n: u8, acc: u16) -> u16 {
    if n == 0 {
        return acc
    }
    return simple_tail(n - 1, acc + n)  // Direct tail call
}

fun main() {
    let result = simple_tail(10, 0)
}