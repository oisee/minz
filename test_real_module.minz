// Test real module loading
import zx.screen;

fun main() -> void {
    // Use actual functions from the module
    let addr = screen.pixel_addr(10, 20);
    @print("Pixel address: ");
    @print(addr);
    @print("\n");
    
    // Use constants
    let red = screen.RED;
    @print("RED color value: ");
    @print(red);
}