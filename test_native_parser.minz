// Simple test file for native parser
fun fibonacci(n: u8) -> u8 {
    if n <= 1 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

fun main() -> void {
    let result = fibonacci(10);
    print_u8(result);
}