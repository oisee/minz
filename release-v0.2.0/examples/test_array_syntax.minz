// Test array syntax parsing

struct Item {
    id: u8,
    value: u16
}

// Test old syntax
let old_array: [10]u8;

// Test new syntax with primitive type
let new_array: [u8; 10];

// Test new syntax with struct type
let items: [Item; 5];

fun main() -> void {
    // Just declare to test parsing
    old_array[0] = 42;
    new_array[0] = 43;
    items[0].id = 1;
}