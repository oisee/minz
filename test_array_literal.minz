// Test array literals [1,2,3] syntax
fun main() -> void {
    // Test array literal initialization
    let numbers = [1, 2, 3, 4, 5];
    
    // Access elements
    let first = numbers[0];
    let second = numbers[1];
    
    // Modify element
    numbers[2] = 10;
    
    // Use in loop
    for i in 0..5 {
        let val = numbers[i];
    }
}

// Test with different types
fun test_types() -> void {
    let bytes = [10, 20, 30];  // u8 array
    let words = [1000, 2000, 3000];  // u16 array
    let bools = [true, false, true];  // bool array
}