// Debug global variable issue
module test_global_init;

global g_byte: u8 = 42;

fn main() -> void {  // Using 'fn' like the original
    // Try different access patterns
    let x = g_byte;  // This should work
    @print("Value: {}\n", x);
}