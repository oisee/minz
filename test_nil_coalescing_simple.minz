// Simple test for nil coalescing operator (??)

fun maybe_fail() -> u8? {
    return 42;
}

fun main() -> void {
    let safe_result = maybe_fail() ?? 0;
    let chained = maybe_fail() ?? maybe_fail() ?? 99;
}