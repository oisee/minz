; Test memory indirect only
    ORG $8000

main:
    LD HL, $1234       ; This should work (immediate)
    LD A, $56          ; This should work (immediate)
    ; Skip the problematic register indirect for now
    ; LD A, (HL)       ; Skip this
    
    RET
    
    END main