// Minimal loop test

struct Item {
    value: u8
}

let items: [Item; 3];

fn main() -> void {
    // Initialize
    items[0].value = 10;
    items[1].value = 20;
    items[2].value = 30;
    
    // Test loop with INTO mode
    loop items into item {
        item.value = item.value + 1;
    }
}