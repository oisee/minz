// Test struct support

type Point = struct {
    x: u8,
    y: u8
};

fn test_struct() -> void {
    let mut p: Point;
    p.x = 10;
    p.y = 20;
    
    let a: u8 = p.x;
    let b: u8 = p.y;
    
    return;
}

fn main() -> void {
    test_struct();
    return;
}