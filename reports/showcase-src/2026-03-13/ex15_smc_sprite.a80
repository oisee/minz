; generated by MIR2 Z80 codegen

; fun draw_row() ; clobbers: C, HL
draw_row:
    LD HL, 0          ; [patch_slot init=0]
draw_row$r0$imm  EQU $-2
    LD (HL), 195
    INC HL
    LD (HL), 60
    RET

; synthesised @smc patcher — fun draw_row_set_r0(v: u16 = HL)
; patches imm16 at draw_row$r0$imm
draw_row_set_r0:
    LD A, L
    LD (draw_row$r0$imm), A
    LD A, H
    LD (draw_row$r0$imm+1), A
    RET

; fun draw_row4() ; clobbers: C, HL
draw_row4:
    LD HL, 0          ; [patch_slot init=0]
draw_row4$r0$imm  EQU $-2
    LD (HL), 255
    INC HL
    LD (HL), 129
    INC HL
    LD (HL), 129
    INC HL
    LD (HL), 255
    RET

; synthesised @smc patcher — fun draw_row4_set_r0(v: u16 = HL)
; patches imm16 at draw_row4$r0$imm
draw_row4_set_r0:
    LD A, L
    LD (draw_row4$r0$imm), A
    LD A, H
    LD (draw_row4$r0$imm+1), A
    RET

