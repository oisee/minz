; generated by MIR2 Z80 codegen

; fun main() ; clobbers: A, HL
main:
    LD A, 65
    CALL console_log
    LD A, 66
    CALL console_log
    LD A, 10
    CALL console_log
    LD A, 69
    CALL console_err
    LD A, 82
    CALL console_err
    LD A, 82
    CALL console_err
    LD A, 10
    CALL console_err
    LD HL, _mir2_str_0
.print_str_0:
    LD A, (HL)
    AND A
    JR Z, .print_str_done_0
    OUT (0x23), A
    INC HL
    JR .print_str_0
.print_str_done_0:
    XOR A
    DI
    HALT

; fun console_log(n: u8 = C)
console_log:
    OUT (0x23), A
    RET

; fun console_err(n: u8 = C)
console_err:
    OUT (0x25), A
    RET

; fun print_str(s: u16 = HL)
print_str:
    LD A, (HL)
        OR A
        JR Z, _ps_done
_ps_loop:
        OUT (0x23), A
        INC HL
        LD A, (HL)
        OR A
        JR NZ, _ps_loop
_ps_done:
    RET

; strings
_mir2_str_0:
    DB 115, 97, 109, 101, 32, 116, 104, 105, 110, 103, 33, 10, 0
