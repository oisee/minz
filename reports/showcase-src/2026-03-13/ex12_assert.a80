; generated by MIR2 Z80 codegen

; fun double(n: u8 = A) -> u8 = A
double:
    ADD A, A
    RET

; fun clamp(v: u8 = A, lo: u8 = B, hi: u8 = C) -> u8 = A ; clobbers: F
clamp:
    CP B
    JRS NC, .clamp_if_join2
.clamp_if_then1:
    LD A, B
    RET
.clamp_if_join2:
    CP C
    JRS Z, .clamp_cret0
    JRS C, .clamp_cret0
.clamp_if_then3:
    LD A, C
    RET
.clamp_cret0:
    RET

; fun abs_diff(a: u8 = A, b: u8 = C) -> u8 = A ; clobbers: F
abs_diff:
    SUB C
    JRS C, .abs_diff_if_join2
.abs_diff_if_then1:
    RET
.abs_diff_if_join2:
    NEG
    RET

