; generated by MIR2 Z80 codegen

; fun Dog_speak(self: Dog = mem) -> u8 = A
Dog_speak:
    LD A, 1
    RET

; fun Cat_speak(self: Cat = mem) -> u8 = A
Cat_speak:
    LD A, 2
    RET

; fun demo() -> u8 = A ; clobbers: HL
demo:
    LD HL, g_dog
    JP Dog_speak

; globals
g_dog:
g_cat:
