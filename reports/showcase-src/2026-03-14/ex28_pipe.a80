; generated by MIR2 Z80 codegen

; fun add_acc(acc: u8 = A, x: u8 = C) -> u8 = A
add_acc:
    ADD A, C
    RET

; fun sum_doubled() -> u8 = A ; clobbers: B, C, F
sum_doubled:
    LD A, 0
    LD C, 5
    SCF
    JRS NZ, .sum_doubled_trmp0
    JRS .sum_doubled_rng_exit2
.sum_doubled_rng_body1:
    LD E, A
    LD A, B
    ADD A, B
    ADD A, E
    DJNZ .sum_doubled_rng_body1
.sum_doubled_rng_exit2:
    RET
.sum_doubled_trmp0:
    LD B, 5
    JRS .sum_doubled_rng_body1

; fun sum_tripled() -> u8 = A ; clobbers: B, C, F
sum_tripled:
    LD A, 0
    LD C, 3
    SCF
    JRS NZ, .sum_tripled_trmp0
    JRS .sum_tripled_rng_exit2
.sum_tripled_rng_body1:
    LD E, A
    LD A, B
    ADD A, B
    INC A
    ADD A, E
    DJNZ .sum_tripled_rng_body1
.sum_tripled_rng_exit2:
    RET
.sum_tripled_trmp0:
    LD B, 3
    JRS .sum_tripled_rng_body1

; fun lambda_0(x: u8 = A) -> u8 = A
lambda_0:
    ADD A, A
    RET

; fun lambda_1(x: u8 = A) -> u8 = A ; clobbers: C
lambda_1:
    INC A
    RET

