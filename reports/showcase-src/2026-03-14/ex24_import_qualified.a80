; generated by MIR2 Z80 codegen

; fun mathlib$ops$add(a: u8 = A, b: u8 = C) -> u8 = A
mathlib_ops_add:
    ADD A, C
    RET

; fun mathlib$ops$double(x: u8 = A) -> u8 = A
mathlib_ops_double:
    ADD A, A
    RET

; fun mathlib$ops$triple(x: u8 = A) -> u8 = A ; clobbers: B
mathlib_ops_triple:
    ADD A, A
    LD B, A
    ADD A, B
    RET

; fun compute() -> u8 = A
compute:
    LD A, 25
    RET

