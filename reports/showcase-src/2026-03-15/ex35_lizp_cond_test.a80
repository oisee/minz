; generated by MIR2 Z80 codegen

; fun main() ; clobbers: A, C, D, F, HL
main:
    LD A, 42
    CALL classify
    LD A, ?
    LD (score), A
    LD HL, score
    LD C, (HL)
    LD A, C
    CP 2
    JRS NZ, .main_if_join2
.main_if_then1:
    LD HL, score
    LD D, (HL)
    LD C, 1
    LD A, D
    ADD A, 1
    LD (score), A
.main_if_join2:
    LD HL, score
    LD D, (HL)
    LD A, D
    AND A
    LD A, F
    AND A
    AND A
    JRS Z, .main_if_join4
.main_if_then3:
    LD HL, score
    LD D, (HL)
    LD C, 1
    LD A, D
    SUB 1
    LD (score), A
.main_if_join4:
    XOR A
    DI
    HALT

; fun classify(n: u8 = A) -> u8 = A ; clobbers: C, F
classify:
    JRS .classify_if_else3
.classify_if_join2:
    RET
.classify_if_else3:
    CP 10
    JRS NC, .classify_if_else6
.classify_if_then4:
.classify_if_join5:
    JRS .classify_if_join2
.classify_if_else6:
    CP 100
    JRS NC, .classify_if_else9
.classify_if_then7:
.classify_if_join8:
    JRS .classify_if_join5
.classify_if_else9:
    JRS .classify_if_join8

; globals
score:
    DB 0
