; generated by MIR2 Z80 codegen

; fun main() -> u8 = A ; clobbers: C, HL
main:
    LD HL, 32768
    LD (HL), 42
    LD HL, 32768
    LD A, (HL)
    RET

; fun peek(addr: u16 = HL) -> u8 = A
peek:
    LD A, (HL)
    RET

; fun poke(addr: u16 = HL, val: u8 = C)
poke:
    LD (HL), C
    RET

