// Test Lua in constants without blocks

const PLATFORM_BITS: u8 = @lua(8);
const MAGIC_NUMBER: u16 = @lua(42 * 100);

fun main() -> void {
    let x: u8 = PLATFORM_BITS;
    let y: u16 = MAGIC_NUMBER;
    return;
}